<Window x:Class="Password.PasswordsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Password"
        mc:Ignorable="d"
        Title="PasswordsWindow" Height="300" Width="400"> 
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>


        <StackPanel Grid.Column="0" Grid.Row="0" Name="panel" Grid.ColumnSpan="2"></StackPanel>
        <Button Grid.Column="0" Grid.Row="1" Name="AddPassButton" Content="Add New Password" Width="150" Height="25" Margin="10" HorizontalAlignment="Left" Click="AddPassButton_Click"/>
        <Button Grid.Column="1" Grid.Row="1" Name="SaveAllBtn" Content="Save All" Width="150" Height="25" Margin="10" HorizontalAlignment="Right" Click="SaveAllBtn_Click"/>
    </Grid>
</Window>
